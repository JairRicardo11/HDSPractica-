version: '3'

services:
  backend:
    build: ./Backend
    ports:
      - "3030:3030"
    volumes:
      - ./Backend:/app
    restart: always

  frontend:
    build: ./Frontend\HDSPRACTICA-
    ports:
      - "8080:80"
    restart: always
